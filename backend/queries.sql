-- The canonical, app-aligned schema now lives in backend/db/schema.sql and is
-- applied via `npm run migrate`. This file is kept only as a pointer to avoid a
-- second, drifting copy of the schema.
--
-- Apply the schema:
--   cd backend && npm run migrate
--
-- See backend/db/schema.sql for the table definitions.
