{
  "name": "medichain",
  "version": "1.0.0",
  "private": true,
  "description": "MediChain — blockchain-anchored medical report validation with AI analysis.",
  "license": "MIT",
  "scripts": {
    "up": "docker compose up --build",
    "down": "docker compose down",
    "logs": "docker compose logs -f"
  }
}
