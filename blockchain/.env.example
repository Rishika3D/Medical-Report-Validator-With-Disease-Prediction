# Only needed to deploy to a public network (e.g. Sepolia). Local Hardhat
# (`npx hardhat node`) needs none of these.

# JSON-RPC URL from Alchemy/Infura
RPC_URL=

# Deployer private key (NEVER commit a real key)
PRIVATE_KEY=
